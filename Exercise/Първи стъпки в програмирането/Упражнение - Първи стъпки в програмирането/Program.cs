using System;

namespace Упражнение___Първи_стъпки_в_програмирането
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
             zad1
            double usd = 1.79549;
            double bgn = double.Parse(Console.ReadLine());
            double convertUsd = usd * bgn;
            Console.WriteLine(convertUsd);
            
            zad 2
            double rad = double.Parse(Console.ReadLine());
            double degrees = rad * 180 / Math.PI;
            Console.WriteLine(degrees);

            //zad3
            double depositedSum = double.Parse(Console.ReadLine());
            int deadLine = int.Parse(Console.ReadLine());
            double yearlyPercent = double.Parse(Console.ReadLine());
            double lihva = depositedSum * yearlyPercent/100;
            double lihvaPerMonth = lihva / 12;
            double sum = depositedSum + deadLine * lihvaPerMonth;
            Console.WriteLine(sum);
            
            zad4
            int numberOfPages = int.Parse(Console.ReadLine());
            int pagesPerHour = int.Parse(Console.ReadLine());
            int numberOfDays = int.Parse(Console.ReadLine());
            int timeForReading = numberOfPages / pagesPerHour;
            int neededHoursPerDay = timeForReading / numberOfDays;
            Console.WriteLine(neededHoursPerDay);
            
            zad 5
            double packetPens = 5.80;
            double packetMarkers = 7.20;
            double spray = 1.20;//per liter
            int numberOfPacketPens = int.Parse(Console.ReadLine());
            int numberOfPacketMarkers = int.Parse(Console.ReadLine());
            int literSpray = int.Parse(Console.ReadLine());
            int percentDiscount = int.Parse(Console.ReadLine());
            double suma = numberOfPacketPens * 5.80 + numberOfPacketMarkers * 7.20 + literSpray * 1.20;
            double endSum = suma - (suma * percentDiscount/100);
            Console.WriteLine(endSum);
            
            zad6
            int safetyNylon = int.Parse(Console.ReadLine());
            int paint = int.Parse(Console.ReadLine());
            int paintTinner = int.Parse(Console.ReadLine());
            int hours = int.Parse(Console.ReadLine());
            double paintPrice = (paint + (11*0.1)) * 14.5;
            double nylonPrice = (safetyNylon + 2) * 1.5;
            double tinnerPrice = paintTinner * 5;
            double bagPrice = 0.4;
            double Sum = nylonPrice + paintPrice + tinnerPrice + bagPrice;
            double priceForBuilders = (Sum * 0.3) * hours;
            double endSum = Sum + priceForBuilders;
            Console.WriteLine(endSum);
            
            zad7
            int numberOfChickenMenu = int.Parse(Console.ReadLine());
            int numberOfRibsMenu = int.Parse(Console.ReadLine());
            int numberOfVegetarianMenu = int.Parse(Console.ReadLine());
            double shipment = 2.5;
            double chickenMenuPrice = 10.35;
            double ribsMenuPrice = 12.4;
            double vegetarianMenuPrice = 8.15;
            double sumMenu = (chickenMenuPrice * numberOfChickenMenu) + (ribsMenuPrice *  numberOfRibsMenu) + (vegetarianMenuPrice * numberOfVegetarianMenu);
            double dessert = 0.2 * sumMenu;
            double finalSum = shipment + (chickenMenuPrice * numberOfChickenMenu) + (ribsMenuPrice * numberOfRibsMenu) + (numberOfVegetarianMenu * vegetarianMenuPrice) + dessert;
            Console.WriteLine("Krainata cena e: " + finalSum);
            

            //zad8
            int priceForYear = int.Parse(Console.ReadLine());
            double basketballShoes = priceForYear - (0.4 * priceForYear);
            double basketballClothes = basketballShoes - (0.2 * basketballShoes);
            double ball = 0.25 * basketballClothes;
            double basketballAccessories = 0.2 * ball;
            double expenses = priceForYear + basketballShoes + basketballClothes + ball + basketballAccessories;
            Console.WriteLine(expenses);

            //zad9
            int lenght = int.Parse(Console.ReadLine());
            int width = int.Parse(Console.ReadLine());
            int height = int.Parse(Console.ReadLine());
            double percent = double.Parse(Console.ReadLine());
            double volume = lenght * width * height;
            double volumeInLiters = volume * 0.001;
            double usedPercent = percent / 100;
            double neededLiters = volumeInLiters * (1 - usedPercent);
            Console.WriteLine(neededLiters);
            */
        }
    }
}
